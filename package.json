{
  "name": "ledkasa-corporate-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "assets:optimize": "node scripts/optimize-assets.mjs",
    "preview": "astro preview",
    "test": "node --test --test-concurrency=1 tests/*.test.mjs"
  },
  "devDependencies": {
    "@astrojs/sitemap": "3.7.3",
    "astro": "^5.13.0",
    "sharp": "0.35.3"
  }
}
